1. Consolidate the environment setup to a single wrapper class. 
2. Consolidate the physical property normalization to a single place that is called for all rollout and training needs. dataset.py is not the right place.

